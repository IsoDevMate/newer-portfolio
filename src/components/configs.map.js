export const mapConfig = {
    defaultLocation: "KAHAWA WENDANI",
    defaultZoom: 14,
    mapStyles: {
        width: 600,
        height: 400,
        aspectRatio: "16/9",
    },
};
// You can also add custom map styles if needed
export const customMapStyle = {
    filter: 'grayscale(1) invert(0.9)',
    mixBlendMode: 'luminosity',
};
